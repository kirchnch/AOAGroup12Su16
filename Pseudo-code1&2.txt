Algorithm 1: EnumerationMaxSubarray(a[1,...,n]) 	for each pair (i,j) with 1 ² i ² j ² n       		compute a[i]+a[i+1]+á á á +a[j-1]+a[j]       		keep max sum found so far       	return max sum foundAlgorithm 2: Better EnumerationMaxSubarray(a[1,...,n])       	for i = 1, ..., n       		sum = 0       		for j = i, ..., n       			sum = sum + a[j]      			keep max sum found so far      	return max sum found